<Window x:Class="DDNetNW.AddNicknameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DDNetNW - Add nickname" Height="320" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#07111F">
    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Foreground" Value="#F3F7FF"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,11"/>
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#143B67"/>
            <Setter Property="BorderBrush" Value="#326AA5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" CornerRadius="12" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1D538E"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#4DA3FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="18" CornerRadius="18" Background="#0B1628" BorderBrush="#244A78" BorderThickness="1" Padding="28">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBlock Text="Add nickname" Foreground="#F3F7FF" FontSize="26" FontWeight="Bold"/>
                <TextBlock Text="Enter the exact DDNet nickname to watch." Foreground="#8FA3BF" FontSize="14" Margin="0,6,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,22,0,0">
                <TextBlock Text="Nickname" Foreground="#8FA3BF" FontSize="13" Margin="2,0,0,8"/>
                <Border CornerRadius="12" Background="#07111F" BorderBrush="#244A78" BorderThickness="1" Padding="16,0">
                    <TextBox x:Name="NicknameBox" Background="Transparent" BorderThickness="0" Foreground="#F3F7FF" CaretBrush="#3A86FF" FontSize="18" Height="44" VerticalContentAlignment="Center"/>
                </Border>
                <TextBlock x:Name="ErrorText" Text="" Foreground="#FF4F6D" FontSize="13" Margin="2,8,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="Cancel" Style="{StaticResource DialogButton}" Background="#0D1D33" BorderBrush="#244A78" Margin="0,0,10,0" Click="Cancel_Click"/>
                <Button Content="Add" Style="{StaticResource DialogButton}" Click="Add_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
